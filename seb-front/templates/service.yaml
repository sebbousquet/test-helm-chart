apiVersion: v1
kind: Service
metadata:
  name: {{ include "seb-front.fullname" . }}
  labels:
    {{- include "seb-front.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seb-front.selectorLabels" . | nindent 4 }}

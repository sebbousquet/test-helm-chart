apiVersion: v1
kind: Service
metadata:
  name: {{ include "seb-proc.fullname" . }}
  labels:
    {{- include "seb-proc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seb-proc.selectorLabels" . | nindent 4 }}

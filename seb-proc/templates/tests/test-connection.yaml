apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "seb-proc.fullname" . }}-test-connection"
  labels:
    {{- include "seb-proc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "seb-proc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "seb-services.fullname" . }}
  labels:
    {{- include "seb-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seb-services.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "seb-services.fullname" . }}-test-connection"
  labels:
    {{- include "seb-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "seb-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
